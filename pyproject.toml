[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "antigravity-cli-unlocker"
version = "1.0.1"
description = "Cross-Platform Split-Tunnel Proxy & Binary Patcher for Google Antigravity CLI (agy)"
readme = "README.md"
authors = [{ name = "NakishN" }]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
    "cryptography>=3.4",
]

[project.scripts]
antigravity-unlock = "antigravity_unlock.cli:main"

[tool.setuptools.packages.find]
include = ["antigravity_unlock*"]

[tool.setuptools.package-data]
antigravity_unlock = ["*.json"]
